https://navermaps.github.io/android-map-sdk/guide-ko/
https://github.com/navermaps/android-map-sdk
https://developers.naver.com/forum/posts/24571
https://navermaps.github.io/maps.js.ncp/docs/tutorial-2-Marker.html
https://3uomlkh.tistory.com/94

특수문자 변환
https://www.baraverkstad.se/encode/

팀 컬러
#F7DE6D

주황
#F68A1D

초록
#39E29B

핑꾸
#F537BA

슬라이드 애니메이션 효과
binding.containerLinearSendFile.setVisibility(View.VISIBLE);
                Animation slideUpAnimation = AnimationUtils.loadAnimation(this, R.anim.slide_up);

축적에 따른 조건절 변경하기
WHERE (
    CASE 
        WHEN #{zoom_level} > 10 THEN C.DISTANCE < 10 -- 예시 축적 레벨에 따라 조건 설정
        WHEN #{zoom_level} <= 10 THEN C.DISTANCE < 5
    END
)
AND ROWNUM < 20;

01098765678
conan1
conan1234!



DECLARE
    v_condition BOOLEAN := TRUE; -- 조건 설정, 실제로는 조건에 맞게 값을 설정해야 합니다
BEGIN
    IF v_condition THEN
        INSERT INTO your_table (column1, column2, ...)
        VALUES (value1, value2, ...);
    END IF;
END;
/


이름 주소 전화 좋아요
